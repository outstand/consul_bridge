source 'https://rubygems.org'

# Specify your gem's dependencies in consul_bridge.gemspec
gemspec

# docker-api with support for more recent docker events, useful for debugging
# gem 'docker-api', github: 'outstand/docker-api', branch: 'more-event-fields'
